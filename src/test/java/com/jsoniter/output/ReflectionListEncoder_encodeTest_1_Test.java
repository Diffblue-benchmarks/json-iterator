package com.jsoniter.output;

import com.fasterxml.jackson.databind.type.ArrayType;
import com.jsoniter.output.JsonStream;
import com.jsoniter.output.ReflectionListEncoder;
import java.lang.reflect.Type;
import org.junit.Assert;
import org.junit.Test;
import org.junit.rules.Timeout;
import org.powermock.reflect.Whitebox;

public class ReflectionListEncoder_encodeTest_1_Test {
//failed_run   @Test
//  public void encodeTest() throws Exception {
//    // Arrange
//    ReflectionListEncoder reflectionListEncoder = Whitebox.newInstance(ReflectionListEncoder.class);
//    String obj = "aaaaa";
//    JsonStream jsonStream = (JsonStream) null;
//
//    // Act
//    reflectionListEncoder.encode(obj, jsonStream);
//
//    // Assert
//    Assert.assertEquals(null, jsonStream);
//  }
  @org.junit.Rule
  public Timeout timeout = new Timeout(10000);
}
